# Local
local   all             all                                     trust

# IPv4
host    all             all             127.0.0.1/32            trust

# Replica user access from replica to master
host    replication     {{ replication_user }}   {{ master_ip }}/32    md5
